{
    "version": 2,
    "buildCommand": "npm run build && npm run vercel-build",
    "functions": {
        "api/server.js": {
            "runtime": "nodejs16.0"
        }
    },
    "routes": [
        {
            "src": "/(.*)",
            "dest": "api/server.js"
        }
    ]
}
