{
    "version": 2,
    "buildCommand": "npm run build && npm run vercel-build",
    "outputDirectory": "/dist",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/"
        }
    ]
}